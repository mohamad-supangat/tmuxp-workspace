session_name: "Mariadb"
start_directory: "~/projects/docker-composes/mariadb/"

windows:
  - layout: main-vertical
    panes:
      - shell_command:
          - docker start mariadb-db-1 adminer-adminer-1
